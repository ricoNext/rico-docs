# Final image generation prompt (reproducibility record)

16:9 article cover illustration. Monochrome only: white or off-white background, black and dark gray linework, optional medium gray for secondary elements. No color. No photorealistic humans.

Style: conceptual editorial cover, hand-drawn sketch aesthetic — organic slightly imperfect pencil/ink lines, variable stroke weight, subtle paper grain texture, light hatching for shading. Not flat corporate vector; not 3D.

Main visual metaphor: the command-line interface as the new connection layer for AI agents — a sketchy terminal window with a simple prompt (`>` or `$`) and a few abstract command tokens, connected by loose hand-drawn arrows and dashed lines to minimal abstract “interface” symbols suggesting API endpoints (simple brackets, small nodes, plug/socket shapes). Suggest programmatic workflow and structured text output without showing real code blocks or illegible tiny text.

Composition: balanced layout with generous whitespace (about 45–55% empty breathing room). Place the illustrated metaphor on the left or right third; keep the opposite side calmer for typography. Single clear focal cluster; avoid clutter.

Typography (must be crisp and readable):

- Primary title in Chinese, large: `CLI 正在成为新的 API`
- Subtitle in English below, smaller: `The CLI is the new API`
- Use clean modern geometric sans-serif letterforms for both lines (not decorative serif). High contrast black on white.

Constraints:

- Do not include watermark, logo, URL, color names, hex codes, or UI brand names.
- No screenshots, no realistic photos, no glossy app mockups.
