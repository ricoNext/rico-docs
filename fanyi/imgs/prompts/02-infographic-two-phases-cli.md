---
illustration_id: 02
type: infographic
style: notion
---

# 两阶段：编程代理 CLI → SaaS 回应

Layout: 横向「时间/阶段」两栏，自上而下阅读；顶部小标题条。

ZONES:

- Zone 1 — 阶段一「AI 编程代理 CLI」：一条时间轴上放置 5 个圆角卡片，**必须包含文章中的产品与大致时间**：Claude Code CLI（2025-05）；Gemini CLI（2025-06）；Codex CLI（2025-04）；Vibe CLI（2025-12）；Copilot CLI（2026-02 公开）。卡片内只写产品名 + 月份/年份，避免伪造其它日期。
- Zone 2 — 阶段二「SaaS 为代理推出 CLI」：第二行卡片列举 **Basecamp CLI（55+ 命令）**、**Stripe Projects CLI**、**Vercel（代理优化命令 + JSON）**、**Polymarket CLI**；可附小字「Google Workspace CLI 触发跟进」作为上下文（不写成长文）。
- Zone 3 — 底部一句：「CLI = 进入智能代理工作流的关键接口层」。

LABELS: 阶段一；阶段二；终端是代理的原生环境；55+ 命令；JSON 输出（与原文一致）

COLORS: 背景 #FAFAFA 或 #FFFFFF；线 #1A1A1A；次线 #4A4A4A；淡蓝 #A8D4F0、淡黄 #F9E79F 用于高亮某一阶段。

Color values (#hex) and color names are rendering guidance only — do NOT display color names, hex codes, or palette labels as visible text in the image.

STYLE: Notion 手绘线稿 + 圆角卡片；图标极简；禁止照片风与 3D。

ASPECT: 16:9

Global: Clean composition with generous white space. Simple or no patterned background. No watermarks, logos, or URLs.
